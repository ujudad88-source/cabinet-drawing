# 함 제작도면 작성기

전기·통신 함체(단자함·분전반) 제작도면을 **현장에서 폰으로 바로 그려 공장에 넘기는** 도구입니다.
손으로 그리던 외형도·정면도·전개도를 치수만 입력하면 A4/A3 도면으로 만들어 줍니다.

설치 없이 브라우저에서 바로 실행되고, 한 번 연 뒤에는 **통신이 끊겨도 동작**합니다.

---

## 빠른 시작

1. 배포 주소를 브라우저에서 엽니다 → `https://<아이디>.github.io/cabinet-drawing/`
2. **아이폰**: 사파리 → 공유(↑) → **홈 화면에 추가** → 아이콘으로 실행
   **안드로이드**: 크롬 메뉴 → **홈 화면에 추가**
3. 치수를 넣고 부속을 얹은 뒤 하단 **공유·저장**으로 카카오톡에 보냅니다

---

## 기능

### 도면
- **7가지 뷰** — 외형도 / 정면도 / 좌측면도 / 우측면도 / 배면도 / 상·하면도 / 전개도
  원하는 것만 켜면 A4 한 장에 자동 배치 (최대 9분할)
- **3D 미리보기** — 여섯 면을 세워 돌려 봅니다. 끌어서 회전, 두 손가락·휠로 확대
- **외형도 방향 선택** — 좌측면 보임 / 우측면 보임
- **실선·점선 정확 구분** — 보이는 모서리는 실선, 가려지는 모서리는 점선
- **카비넷 투상** — 깊이를 실제의 절반 길이로 그려 정육면체가 정육면체로 읽힙니다
- **표제란** — 현장명·도면번호·품명·규격·수량·척도·작성자·작성일·재질·색상·구성·REV

### 함 구조
- **단 구성(세로 연결)** — 본체 + 풀박스 + 2단·3단을 세로로 이어 붙임
- **열 구성(가로 연결)** — 우측으로 함을 이어 붙임
- 이어 붙인 **각 칸은 독립된 함**으로 취급됩니다. 부속은 소속 칸을 가지고,
  좌우 대칭·속판 자동 크기가 모두 그 칸 안에서 계산됩니다
- **전면 테두리(카바)** 유무와 폭 지정
- **단 좌우 마감카바** 표기 (타공후 카바부착)

### 부속 (12종)
환기루버 · 구멍(앙카/피스) · 키(푸쉬버튼/탈착/푸쉬핸들) · 이중시건장치(가로/세로) ·
명판 · 아크릴창 · 엠보타공 · 케이블 인입타공 · 속판(PVC/철/빼끄) · 접지 · 케이블걸이 · 타공덮개

- **기준면 입력** — "좌측면에서 60, 상단에서 60" 식으로 입력. 우측·하단 기준도 선택
- **잠금** — 위치 이탈 방지. 잠겨도 지시선·명칭은 자유롭게 이동
- **그룹** — 여러 부속을 묶어 함께 이동
- **정렬·균등 배치** — 좌·중·우, 상·중·하, 가로 균등, 세로 균등
- **스냅 가이드** — 끌 때 다른 부속 중심선에 자동으로 붙음
- **기본값 저장** — 자주 쓰는 크기·위치를 종류별로 기억
- **점검 경고** — 칸 이탈·겹침을 자동으로 잡아냄
- 속판 3종은 재질 해칭(빗금·점 패턴)으로 한눈에 구분

### 치수·표기
- 전체 치수는 위·왼쪽, 부속 위치 치수는 아래·오른쪽에 배치해 겹치지 않음
- **치수를 누르면** 어느 부속의 어느 방향 치수인지 표시되고 개별로 숨길 수 있음
- 면 단위 치수 일괄 켜기/끄기
- **지시선·명칭을 끌어서** 겹치지 않게 재배치 (지시선이 따라옴)
- 명칭은 원하는 글자로 변경 가능

### 산출·이력
- **가공 지시표** — 부호 / 부착면 / 외·내 / 명칭 / 규격·타공치수 / 위치 / 수량 / 기준
- **집계표** — 함이 여러 개일 때 번호·품명·규격·수량·합계
- **리비전** — 기록 시점과의 차이를 항목별로 비교. 표제란에 `REV.n`, 바뀐 부속에 △R 표시
- **발송 이력** — 공유·저장할 때마다 자동 기록 (날짜·함·REV·받는 곳·파일명)

### 내보내기
| 형식 | 용도 |
|---|---|
| **PNG** | 카톡에 바로 붙습니다. 함이 여러 개면 여러 장 한 번에 |
| **PDF** | A4/A3 여러 장. 집계표 + 도면 + 가공표 전체 묶음 |
| **SVG** | 진짜 벡터. 확대해도 안 깨지고 캐드·일러스트에서 열립니다 |
| **작업 파일(.json)** | 백업·기기 이전·동료 전달. 불러오면 그대로 복원 |

- A4와 A3는 가로세로비가 같아 **같은 이미지가 양쪽에 꼭 맞게** 인쇄됩니다
- 용지 설정에 따라 해상도가 달라집니다 (A4 299dpi / A3 254dpi)

### 조작
- **자유 확대·이동** — 20%~1200%, 두 손가락·두 번 두드리기·휠·끌기
- **도면 전체화면** (⛶) — 폰에서 도면만 화면 가득
- **되돌리기** 40단계
- 부속 검색 · 면별 필터 · 섹션 접기
- 넓은 화면에서는 왼쪽 도면 고정 + 오른쪽 입력 2단 배치

---

## 배포 방법

`배포방법.md` 참고. 요약하면 —

1. 깃허브에 Public 저장소를 만들고 **아래 5개 파일을 올립니다**
2. `Settings → Pages → Deploy from a branch → main / (root)` → Save
3. 1~2분 뒤 `https://<아이디>.github.io/<저장소>/` 로 열립니다

### 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 본체. 도면 엔진·UI·내보내기 전부 |
| `manifest.webmanifest` | 홈 화면 앱 정보 |
| `sw.js` | 오프라인 캐시 |
| `icon-180.png` `icon-512.png` | 홈 화면 아이콘 |
| `build.py` | 빌드 스크립트 (아래 참고) |
| `README.md` `배포방법.md` | 문서 |

### 빌드

`index.html`은 아티팩트용 소스에 문서 껍데기(viewport·매니페스트·서비스워커 등록)를
씌워 만듭니다. 소스를 고친 뒤 아래를 실행하면 배포용 5개 파일이 다시 생성됩니다.

```
python build.py
```

> **viewport 메타는 반드시 있어야 합니다.** 없으면 모바일이 화면 폭을 980px로
> 가정하고 축소해 글자가 아주 작게 보입니다. build.py가 자동으로 넣습니다.

---

## 알려진 제한

- **전개도는 절곡 여유(bend allowance)가 반영되지 않았습니다.** 참고용이며,
  재단 치수는 공장 계산에 맡겨야 합니다
- **데이터는 브라우저에 저장됩니다.** 기록 삭제·저장공간 정리로 지워질 수 있으니
  중요한 작업은 **작업 파일로 내보내 두세요**
- 기기 간 자동 동기화는 없습니다 (작업 파일로 옮깁니다)
- 되돌리기는 40단계이며 다시 실행(redo)은 없습니다
- 부속 기호는 실제 부품을 본떠 그린 것으로, 형상이 100% 같지는 않습니다

---

## 앞으로 계획

- [ ] DXF 내보내기 — 공장이 캐드로 바로 열어 재단·타공
- [ ] 절곡 여유 계산 — 실제 재단 치수 산출
- [ ] 클라우드 동기화 — 폰↔PC 자동
- [ ] 지시선 자동 겹침 회피
- [ ] 부속 라이브러리 확장 (실제 도면에서 추출한 형상)

---

## 변경 이력

> 패치할 때마다 이 항목 맨 위에 추가합니다. 별도 파일을 만들지 않습니다.

### v15 — 2026-09-22
- **홈 화면** — 앱을 열면 `손도면 제작` / `캐비닛 랙 실장` 두 갈래. 랙 실장은 개발 예정 화면(계획 안내)
- **제작기는 항상 초기화 상태로 시작** — 부속 없는 본체 하나. 직전 작업은 지우지 않고 보관해서
  홈의 `직전 작업 이어서 하기`로 불러옴. 제작기 상단 `⌂ 홈`으로 돌아가기
- **초기화 버튼** — 되돌리기 옆 `⟲ 초기화`. 확인 후 처음 상태로, 되돌리기로 복구 가능
- **카바 열기 강조** — 3D 탭에서 주황색 알약 버튼 + 세 번 깜빡임, 열리면 흰 바탕으로 바뀜
- **카바 절곡 깊이 표시** — 카바만 선택 시 나오던 숫자 `25`에 이름·단위(mm)·`?` 설명 추가
- **경첩 모양 표시 제거** — 좌·우 경첩 선택 시 정면도에 그리던 경첩 사각형 삭제 (양개 가운데 선만 유지)
- **단 좌우 마감카바 삭제** — 체크박스와 외형도의 X 표시·주기 제거

### v14 — 2026-09-22
- **3D 미리보기가 탭·버튼을 덮던 문제 수정** — 3D 화면의 기준 위치가 없어 폰에서
  화면 전체로 펼쳐지던 것. 이제 도면 영역 안에만 그려짐
- **카바 경첩 선택** — 좌측 / 우측 / 양개. 정면도에 경첩 표시, 양개는 가운데 맞닿는 선.
  3D에서는 **칸마다 카바가 따로** 열리고 경첩 방향대로 회전
- **함 구성 순서 변경** — 열(가로 연결)이 위, 단(세로 연결)이 아래
- **함 구성 도식** — 열·단을 실제 비율로 그린 미니 도식. 칸을 누르면 그 치수 입력칸으로 이동,
  ＋열 / ＋단 버튼으로 바로 추가
- **제작 범위** — 함 전체 / **내함만**(전면은 개구부, 카바 부속 제외, 도어 전개 없음) /
  **카바만**(전면 부속만, 절곡 깊이 입력, 카바 전면을 중심으로 전개).
  표제란·집계표 품명에 (내함)·(카바) 표기. 제외된 부속은 지우지 않고 목록에 "도면 제외" 표시
- **도면 메모 강조 방식** — ⚠ 주의(이중 테두리 + 경고 삼각형) / ☁ 구름(리비전 클라우드) /
  ▭ 박스. 글자 크기 보통·크게. 흑백 인쇄에서도 모양으로 구분됨. 새 메모 기본값은 주의·크게

### v13 — 2026-09-22
- **확대해도 글자·선이 깨지지 않음** — 확대 방식을 그림 늘리기(CSS 변형)에서
  SVG 보이는 범위(viewBox) 바꾸기로 교체. 오토캐드처럼 매번 벡터로 다시 그림.
  최대 2400%, 도면이 화면에서 완전히 사라지지 않는 한 어디로든 이동
- **아이폰 홈 화면 앱 상단 가림 수정** — 상태바가 제목·메뉴를 덮던 문제.
  상태바 스타일을 `default`로 바꾸고 안전영역(safe-area) 여백 반영
- **부속 삭제** — 목록 각 줄에 ✕ 개별 삭제, 선택 후 `선택 삭제`,
  `보이는 것 전체 선택`. 되돌리기로 복구 가능
- **표제란 겹침 수정** — 입력칸: 아이폰 날짜 칸이 옆 칸을 덮던 문제(2열 배치로 변경).
  도면 표제란: 작성일을 독립 칸으로 분리(수량·척도·작성자·작성일), REV 날짜 위치 조정
- **외형도에 가려진 면의 부속 표시** — 배면·가려진 측면·하판의 부속을 파선으로.
  배면 안쪽에 속판을 붙이면 외형도에서 보임
- **3D 카바 열기** — 좌측 경첩으로 카바가 열리고, 안쪽 면(상·하·좌·우·배면)에
  내부 부착 부속이 표시됨. 카바 바깥은 전면 부속, 카바 안쪽은 전면 내부 부착 부속
- **이어 붙인 함 전개도** — 하나의 함으로 뭉쳐 나오던 것을 **칸마다 따로 전개**.
  바깥으로 드러나는 면의 부속만 해당 칸 날개에 표시

### v12 — 2026-09-16
- **깃허브 배포 정식 지원** — viewport 메타 누락으로 모바일에서 글자가 아주 작게
  보이던 문제 해결. 문서 껍데기를 씌우는 빌드 스크립트 도입
- **오프라인 실행(PWA)** — 서비스워커·매니페스트·아이콘 추가. 홈 화면 앱으로 설치되고
  통신이 끊겨도 실행됨
- **3D 미리보기** — 여섯 면을 세워 돌려 보는 탭 추가. 라이브러리 없이 CSS 3D로 구현
- **도면 전체화면(⛶)** — 폰에서 도면만 화면 가득
- **작업 파일 내보내기·불러오기(.json)** — 백업·기기 이전·동료 전달.
  파일의 함만 현재 작업에 추가하는 것도 가능
- **SVG(벡터) 내보내기** — 확대해도 안 깨지고 캐드·일러스트에서 열림

### v11 — 2026-09-16
- 도면 화면을 **오토캐드식 자유 확대·이동**으로 교체 (20%~1200%, 어느 방향으로든 이동).
  도면을 다시 그려도 화면 위치가 유지되어 **확대 중 떨림이 사라짐**
- **끌기 방향 버그 수정** — 기준면이 우측/하단인 부속을 끌면 반대로 움직이던 문제
  (속판 상하 반전). 그룹 이동도 각자 기준면에 맞춰 보정
- 부속을 끌 때 화면 전체가 아니라 **도면만 갱신**하도록 바꿔 반응 속도 개선
- **도면 메모 강조색** — 주황 테두리 + 배경으로 검은 선 도면 위에서 바로 식별
- **부속 기본값** — 종류별로 크기·기준면·위치·대칭을 저장해 두고 추가할 때 자동 적용
- 이미지도 **함 전체를 여러 장 한 번에** 공유

### v10 — 2026-09-16
- **카카오톡 공유** — 형식(PNG/PDF)·범위 선택 후 공유 시트로 바로 전송
- 하단 버튼을 `공유·저장` 하나로 통합, 상단을 `⋯ 메뉴`로 정리
- **발송 이력** — 공유·저장 시 자동 기록 (200건)
- **리비전 REV 표기** — 표제란에 `REV.n`과 기록일, 바뀐 부속에 △R
- **부속 검색**, **면 단위 치수 일괄 켜기/끄기**
- 아이폰·공유 도움말을 앱 안에 내장

### v9 — 2026-09-16
- **치수를 눌러 확인·삭제** — 어느 부속의 어느 방향 치수인지 표시, 가로/세로 개별 숨김
- 도면 구성(뷰 선택)을 **도면 바로 아래**로 이동
- **섹션 접기** (상태 기억)
- **리비전** 기록·비교, **칸 통째로 복사**, **부속 그룹**, **점검 경고**, **도면 메모**

### v8 — 2026-09-16
- **칸(cell) 개념 도입** — 이어 붙인 함의 각 칸을 독립된 함으로 취급.
  환기루버 좌우 대칭이 칸 안에서 잡히고, 속판 자동 크기도 칸 기준으로 계산되며,
  명판을 칸마다 따로 배치 가능
- **기준면 입력** — "좌측면에서 / 우측면에서", "상단에서 / 하단에서"
- 명칭을 원하는 글자로 변경 가능
- **상·하면도** 뷰 복원, 가공표 정리, 잠김 안내를 화면 상단 알림으로 강조

### v7 — 2026-09-16
- **열 구성(가로 연결)** — 우측으로 함을 이어 붙임
- **재질·두께 / 색상·도장** 분리 (기본 STEEL 1.6T / 5Y7-1 분체도장)
- **부속 잠금** — 잠겨도 지시선·명칭은 이동 가능
- **면별 필터**, **함 템플릿**, **치수 ± 버튼**, **순서 바꾸기**, **길게 눌러 편집**

### v6 — 2026-09-16
- **정육면체 깊이 비율 수정** — 카비넷 투상(절반 길이)으로 교체
- 확대·축소, 되돌리기, 선택 부속 강조, PC 2단 배치

### v5 — 2026-09-15
- **단 구성(세로 연결)** — 받침대를 풀박스 단으로 전환, 2단·3단 연결 지원
- 화면 확대·축소, **A4/A3 용지 선택**과 해상도 보정
- 속판 3종을 재질 해칭으로 구분

### v4 — 2026-09-15
- 외형도 방향 선택(좌측면/우측면 보임)
- **실선·점선 정확 구분**, 전면 테두리 유무
- 이중시건 가로/세로, **지시선·명칭 이동**

### v3 — 2026-09-15
- 실제 DWG에서 부속 형상 22종 추출 (이후 되돌림)

### v2 — 2026-09-15
- 좌·우측면도 분리, 외부/내부 부착 구분, 가공 지시표, 부호 번호

### v1 — 2026-09-15
- 최초 작성. 외형도·정면도·측면도·전개도, 부속 12종, 표제란, PNG/PDF 내보내기
