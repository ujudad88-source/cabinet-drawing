# 함 제작도면 작성기 — 깃허브 페이지 배포

계정 없는 동료(아이폰 포함)도 주소만으로 쓸 수 있게 올리는 방법입니다.
git 명령 없이 웹사이트에서 클릭만으로 됩니다. 5분 걸립니다.

## 이 폴더에 들어 있는 것

| 파일 | 역할 |
|---|---|
| `index.html` | 본체. **모바일 viewport 설정 포함** |
| `manifest.webmanifest` | 홈 화면에 앱으로 추가될 때 쓰는 정보 |
| `sw.js` | 오프라인 캐시 (통신 없어도 실행) |
| `icon-180.png` / `icon-512.png` | 홈 화면 아이콘 |

**5개 파일을 전부 올려야 합니다.** 하나라도 빠지면 아이콘이나 오프라인이 안 됩니다.

## 순서

1. github.com 가입 후 로그인
2. 우측 상단 `+` → `New repository`
3. Repository name 에 `cabinet-drawing` 입력 → `Public` 선택 → `Create repository`
4. `uploading an existing file` 링크 클릭
5. 이 폴더의 **파일 5개를 한꺼번에 끌어다 놓기** → `Commit changes`
6. 상단 `Settings` → 왼쪽 `Pages`
7. Source 를 `Deploy from a branch`, Branch 를 `main` / `/ (root)` 로 두고 `Save`
8. 1~2분 뒤 주소가 나옵니다:
   `https://<아이디>.github.io/cabinet-drawing/`

이 주소를 카톡으로 보내면 누구나 바로 씁니다.

## 아이폰에서 앱처럼 쓰기

1. 사파리로 위 주소 열기
2. 하단 공유 버튼(↑) → **홈 화면에 추가**
3. 아이콘으로 실행 — 주소창 없이 전체화면, 통신이 끊겨도 실행됩니다

## 수정본을 다시 올릴 때

저장소에서 `Add file` → `Upload files` 로 **같은 이름 파일을 다시 올리면** 덮어써집니다.
1~2분 뒤 반영되고, 이미 홈 화면에 추가한 사람도 앱을 다시 열면 최신본이 내려받아집니다.

> `sw.js` 안의 버전 문자열이 바뀌면 이전 캐시가 자동으로 지워집니다.
> 빌드할 때마다 자동으로 바뀌므로 신경 쓰지 않아도 됩니다.

## 아티팩트 주소와 뭐가 다른가

- **더 좋아지는 것**: 로그인 불필요 / 카톡 공유 확실히 동작 / 오프라인 실행 /
  홈 화면 앱 / 수정 이력이 깃허브에 전부 남음
- **빠지는 것**: 기기 간 클라우드 자동 동기화. 각 기기 브라우저에 따로 저장됩니다.
  (나중에 Firebase 등을 붙이면 되살릴 수 있습니다)

## 주의

Public 저장소이므로 주소를 아는 사람은 누구나 도구를 씁니다.
도구 코드만 공개되는 것이고, 입력한 도면 내용은 각자 브라우저에만 남습니다.
코드 공개가 부담이면 Netlify 나 Cloudflare Pages 로 비공개 저장소도 무료로 배포됩니다.
